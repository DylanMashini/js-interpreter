let x = 5;
let y = 10.5;

if (x > 1) {
    x = x + 1;
    y = y - 1;
}

let z = "Hello World";
let isNight = false;

function calculate(a, b) {
    let result = a * b;
    return result;
}

calculate(12, 12)

if (x == y) {
    x++;
} else {
    x--;
}

for (let i = 0; i < 10; i++) {
    x = x + i;
}

while (y > 0) {
    y = x - 1;
}

let message = 'The value of x is: ' + x;
let comparisonResult = x >= y;
let notEqual = x != y;

let someNull = null;
let someUndefined = undefined;

let andCheck = isNight && (x > 5);
let orCheck = isNight || (y < 5);

let increment = x++;
let decrement = y--;
